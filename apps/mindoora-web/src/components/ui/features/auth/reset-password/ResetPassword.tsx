import { FormWrapper } from "~/components/ui/elements/formInputs/FormWrapper";
import FormResetPassword from "./FormResetPassword";

const ResetPassword = () => {
  return (
    <FormWrapper>
      <FormResetPassword />
    </FormWrapper>
  );
};

export default ResetPassword;
