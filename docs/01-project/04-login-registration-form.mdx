-- they are just Modals
        -- login
        -- registration
        -- verify
-- CheckBox/ PasswordInput/ Button/ Email are Generic
-- GoogleRecapcha
-- useLogin/ useRegister are hooks created with "react-hook-form"
        https://react-hook-form.com/get-started

1. when authenticated modal is null & redirected to /home.
2. !captcha => show error & don't submit the form
